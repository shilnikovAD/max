# 🚀 Подробное руководство по запуску FizTech Tutors

Это полное пошаговое руководство по установке и запуску приложения FizTech Tutors на вашем компьютере.

## 📋 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Установка Node.js](#установка-nodejs)
3. [Клонирование проекта](#клонирование-проекта)
4. [Установка зависимостей](#установка-зависимостей)
5. [Запуск приложения](#запуск-приложения)
6. [Запуск тестов](#запуск-тестов)
7. [Сборка для production](#сборка-для-production)
8. [Решение проблем](#решение-проблем)

---

## 1. Предварительные требования

Перед началом убедитесь, что у вас установлено:

- **Windows 10/11** (для других ОС шаги будут аналогичны)
- **Интернет-соединение** для загрузки зависимостей
- **Текстовый редактор или IDE** (рекомендуется Visual Studio Code или JetBrains WebStorm)

---

## 2. Установка Node.js

Node.js требуется для запуска JavaScript/TypeScript приложений.

### Шаг 2.1: Проверка установки

Откройте PowerShell и выполните:

```powershell
node --version
npm --version
```

Если команды выполнились успешно и показали версии (например, `v18.17.0` и `9.6.7`), переходите к [шагу 3](#клонирование-проекта).

### Шаг 2.2: Загрузка Node.js

Если Node.js не установлен:

1. Откройте браузер и перейдите на [https://nodejs.org/](https://nodejs.org/)
2. Скачайте **LTS версию** (рекомендуется, например Node.js 18.x или 20.x)
3. Запустите установщик `.msi`
4. Следуйте инструкциям установщика:
   - Примите лицензионное соглашение
   - Выберите путь установки (по умолчанию: `C:\Program Files\nodejs\`)
   - Убедитесь, что отмечена опция "Add to PATH"
   - Нажмите "Install"

### Шаг 2.3: Проверка установки

После установки **закройте и откройте заново PowerShell**, затем проверьте:

```powershell
node --version
npm --version
```

Должны отобразиться версии Node.js и npm.

---

## 3. Клонирование проекта

### Вариант A: Через Git (если установлен)

```powershell
cd C:\Users\User\IdeaProjects
git clone https://github.com/shilnikovAD/max.git
cd max
```

### Вариант B: Скачивание ZIP-архива

1. Откройте [https://github.com/shilnikovAD/max](https://github.com/shilnikovAD/max)
2. Нажмите зеленую кнопку **"Code"** → **"Download ZIP"**
3. Распакуйте архив в `C:\Users\User\IdeaProjects\max`
4. Откройте PowerShell и перейдите в папку проекта:

```powershell
cd C:\Users\User\IdeaProjects\max
```

---

## 4. Установка зависимостей

Теперь установим все необходимые библиотеки и пакеты.

### Шаг 4.1: Установка npm пакетов

В папке проекта выполните:

```powershell
npm install
```

Этот процесс может занять 2-5 минут. npm загрузит все зависимости, указанные в `package.json`.

**Что делает эта команда:**
- Скачивает React, TypeScript, Redux Toolkit и другие библиотеки
- Устанавливает инструменты для сборки (Vite)
- Устанавливает тестовые фреймворки (Jest, Playwright)
- Создает папку `node_modules` с ~200MB зависимостей

### Шаг 4.2: Установка браузеров для Playwright (для E2E тестов)

Для запуска end-to-end тестов нужно установить браузеры:

```powershell
npx playwright install
```

Эта команда скачает Chromium, Firefox и WebKit (~300MB).

**Если возникает ошибка** с node.exe, попробуйте:

```powershell
# Перезапустите PowerShell от имени администратора и выполните:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
npm install
npx playwright install
```

---

## 5. Запуск приложения

### Шаг 5.1: Режим разработки (Development)

Для запуска приложения в режиме разработки с hot-reload:

```powershell
npm run dev
```

**Вывод в консоли должен быть примерно таким:**

```
  VITE v5.0.0  ready in 523 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

### Шаг 5.2: Открытие в браузере

Откройте браузер и перейдите по адресу:

```
http://localhost:5173/
```

Вы должны увидеть главную страницу FizTech Tutors с каталогом репетиторов.

### Шаг 5.3: Остановка сервера

Для остановки сервера разработки нажмите в PowerShell:

```
Ctrl + C
```

Подтвердите остановку, если будет запрос.

---

## 6. Запуск тестов

Проект включает три типа тестов:

### 6.1 Unit-тесты (Jest)

Юнит-тесты проверяют отдельные функции и компоненты.

**Запустить все тесты:**

```powershell
npm test
```

**Запустить тесты в режиме watch (автоматический перезапуск при изменениях):**

```powershell
npm run test:watch
```

**Сгенерировать отчет о покрытии кода тестами:**

```powershell
npm run test:coverage
```

Отчет будет создан в папке `coverage/`. Откройте `coverage/lcov-report/index.html` в браузере для просмотра.

### 6.2 Компонентные тесты (Storybook)

Storybook позволяет просматривать компоненты в изоляции.

**Запустить Storybook:**

```powershell
npm run storybook
```

Откроется браузер с адресом `http://localhost:6006/`. Вы увидите:
- Button компонент (различные варианты)
- Input компонент (различные состояния)
- Card компонент

**Собрать статическую версию Storybook:**

```powershell
npm run build-storybook
```

Результат будет в папке `storybook-static/`.

### 6.3 E2E тесты (Playwright)

End-to-end тесты проверяют полные пользовательские сценарии.

**Запустить E2E тесты (headless режим):**

```powershell
npm run e2e
```

**Запустить E2E тесты с UI (интерактивный режим):**

```powershell
npm run e2e:ui
```

Откроется Playwright Test UI, где можно:
- Просматривать все тесты
- Запускать тесты по одному
- Видеть скриншоты и видео при ошибках

**Что тестируют E2E тесты:**
1. ✅ Отображение главной страницы и каталога репетиторов
2. ✅ Навигация на страницу детали репетитора
3. ✅ Фильтрация репетиторов по цене
4. ✅ Переход на форму создания профиля репетитора
5. ✅ Заполнение и отправка формы репетитора
6. ✅ Навигация на страницу "О проекте"
7. ✅ Поиск репетиторов по имени
8. ✅ Добавление и удаление репетитора из избранного
9. ✅ Навигация на страницу избранного
10. ✅ Отображение пустого состояния избранного

---

## 7. Сборка для production

### Шаг 7.1: Создание production сборки

```powershell
npm run build
```

Эта команда:
- Компилирует TypeScript в JavaScript
- Минифицирует код
- Оптимизирует изображения и стили
- Создает папку `dist/` с готовым приложением

### Шаг 7.2: Предварительный просмотр production сборки

```powershell
npm run preview
```

Откройте `http://localhost:4173/` для просмотра production версии.

### Шаг 7.3: Деплой на GitHub Pages

Проект настроен для автоматического деплоя на GitHub Pages через GitHub Actions.

**При push в ветку `main`:**
1. GitHub Actions автоматически запускает сборку
2. Результат публикуется на `https://shilnikovad.github.io/max/`

**Для ручного деплоя:**

```powershell
npm run build
# Затем загрузите содержимое папки dist/ на хостинг
```

---

## 8. Решение проблем

### Проблема: "node.exe не распознано"

**Причина:** Node.js не добавлен в PATH или PowerShell не перезапущен после установки.

**Решение:**
1. Закройте и откройте PowerShell заново
2. Проверьте: `node --version`
3. Если не помогло, переустановите Node.js с опцией "Add to PATH"

### Проблема: "Executable doesn't exist" при запуске Playwright

**Причина:** Браузеры Playwright не установлены.

**Решение:**

```powershell
npx playwright install
```

### Проблема: "Cannot find module" при запуске тестов

**Причина:** Зависимости не установлены или установлены неполностью.

**Решение:**

```powershell
rm -r node_modules
rm package-lock.json
npm install
```

### Проблема: Порт 5173 уже занят

**Причина:** Другое приложение использует порт 5173.

**Решение:**

```powershell
# Остановите процесс на порту 5173
netstat -ano | findstr :5173
# Найдите PID процесса и завершите его:
taskkill /PID <PID> /F

# Или измените порт в vite.config.ts:
# server: { port: 3000 }
```

### Проблема: ESLint или TypeScript ошибки

**Причина:** Несоответствие версий или конфигурации.

**Решение:**

```powershell
# Проверить ошибки TypeScript
npx tsc --noEmit

# Проверить ошибки ESLint
npm run lint

# Автоматически исправить некоторые ошибки
npm run lint:fix
```

### Проблема: "Access denied" при установке пакетов

**Причина:** Недостаточно прав.

**Решение:**

```powershell
# Запустите PowerShell от имени администратора
# Win + X → "Windows PowerShell (Администратор)"
# Затем перейдите в папку проекта и выполните:
npm install
```

### Проблема: Медленная установка зависимостей

**Причина:** Медленное интернет-соединение или проблемы с npm registry.

**Решение:**

```powershell
# Очистите кэш npm
npm cache clean --force

# Используйте другой registry (например, китайский зеркальный)
npm config set registry https://registry.npmmirror.com

# Или вернитесь к официальному
npm config set registry https://registry.npmjs.org/
```

---

## 📚 Дополнительные команды

### Проверка кода

```powershell
# Линтинг
npm run lint

# Автоматическое исправление
npm run lint:fix

# Форматирование кода
npm run format
```

### Работа с зависимостями

```powershell
# Показать устаревшие пакеты
npm outdated

# Обновить все пакеты (осторожно!)
npm update

# Установить конкретный пакет
npm install <package-name>

# Удалить пакет
npm uninstall <package-name>
```

### Очистка проекта

```powershell
# Удалить node_modules и dist
rm -r node_modules, dist

# Удалить кэш
npm cache clean --force

# Переустановить все
npm install
```

---

## 🎯 Быстрый старт (TL;DR)

Если вы опытный разработчик и хотите быстро запустить проект:

```powershell
# 1. Убедитесь, что Node.js 18+ установлен
node --version

# 2. Клонируйте и перейдите в папку
cd C:\Users\User\IdeaProjects\max

# 3. Установите зависимости
npm install

# 4. Установите браузеры для тестов
npx playwright install

# 5. Запустите dev сервер
npm run dev

# 6. Откройте http://localhost:5173/ в браузере
```

---

## 🔗 Полезные ссылки

- **Live Demo**: [https://shilnikovad.github.io/max/](https://shilnikovad.github.io/max/)
- **GitHub Repo**: [https://github.com/shilnikovAD/max](https://github.com/shilnikovAD/max)
- **Node.js**: [https://nodejs.org/](https://nodejs.org/)
- **Vite Docs**: [https://vitejs.dev/](https://vitejs.dev/)
- **React Docs**: [https://react.dev/](https://react.dev/)
- **Redux Toolkit**: [https://redux-toolkit.js.org/](https://redux-toolkit.js.org/)
- **Playwright**: [https://playwright.dev/](https://playwright.dev/)

---

## 💡 Советы для разработки

1. **Hot Reload**: При запуске `npm run dev` изменения в коде автоматически отображаются в браузере
2. **TypeScript**: Используйте типы везде для лучшей поддержки IDE
3. **Redux DevTools**: Установите расширение для браузера для отладки Redux
4. **React DevTools**: Установите расширение для инспектирования React компонентов
5. **Storybook**: Разрабатывайте компоненты изолированно для лучшей переиспользуемости

---

## ✅ Чек-лист перед запуском

- [ ] Node.js 18+ установлен
- [ ] npm установлен
- [ ] PowerShell открыт в папке проекта
- [ ] Выполнен `npm install`
- [ ] Выполнен `npx playwright install` (для E2E тестов)
- [ ] Порт 5173 свободен
- [ ] Интернет-соединение активно (для первого запуска)

---

Если у вас возникли вопросы или проблемы, не описанные в этом руководстве, создайте Issue на GitHub или свяжитесь с командой разработки.

**Удачи в разработке! 🚀**

