# 🔍 Фильтр по предмету - Руководство

## ✅ Что добавлено

Реализован фильтр по предмету на главной странице, дополняющий существующий фильтр по цене.

---

## 🎯 Возможности

### Фильтрация по предмету

Теперь пользователи могут фильтровать репетиторов по преподаваемому предмету:
- **Математика**
- **Физика**
- **Информатика**

### Комбинированная фильтрация

Можно использовать несколько фильтров одновременно:
- ✅ Предмет + Цена
- ✅ Предмет + Поиск
- ✅ Все фильтры вместе

---

## 🚀 Как использовать

### Шаг 1: Откройте главную страницу

```
http://localhost:5173/
```

### Шаг 2: Найдите секцию фильтров

Под hero-секцией видна панель с фильтрами:
```
[Поиск...] [Предмет ▼] [Макс. цена] [Применить фильтры]
```

### Шаг 3: Выберите предмет

1. Кликните на выпадающий список "Все предметы"
2. Выберите нужный предмет:
   - Математика
   - Физика
   - Информатика

### Шаг 4: Примените фильтр

Нажмите кнопку **"Применить фильтры"**

### Результат

✅ Показаны только репетиторы, преподающие выбранный предмет

---

## 📊 Примеры использования

### Пример 1: Найти репетиторов по математике

```
1. Открыть главную страницу
2. Выбрать "Математика" в выпадающем списке
3. Нажать "Применить фильтры"

Результат:
✅ Показан Алексей Иванов (преподает Математику)
❌ Мария Петрова скрыта (преподает только Физику)
❌ Дмитрий Сидоров скрыт (преподает только Информатику)
❌ Екатерина Смирнова скрыта (преподает только Физику)
```

### Пример 2: Найти репетиторов по физике до 2000₽

```
1. Выбрать "Физика"
2. Ввести "2000" в поле "Макс. цена"
3. Нажать "Применить фильтры"

Результат:
✅ Алексей Иванов (Физика + Математика, 2000₽)
✅ Мария Петрова (Физика, 1500₽)
✅ Екатерина Смирнова (Физика, 1800₽)
❌ Дмитрий Сидоров скрыт (Информатика, не Физика)
```

### Пример 3: Сбросить фильтр

```
1. Выбрать "Все предметы" в выпадающем списке
2. Нажать "Применить фильтры"

Результат:
✅ Показаны все репетиторы
```

---

## 🎨 Визуальный дизайн

### Выпадающий список (Select)

**Стили:**
- Ширина: 100% (минимум 200px)
- Padding: 12px 16px
- Border: 2px solid #e0e0e0
- Border-radius: 8px
- Font-size: 16px

**Состояния:**
- **Default:** Серая рамка
- **Hover:** Светло-серая рамка (#cbd5e0)
- **Focus:** Синяя рамка (#667eea) + тень

**Опции:**
- Все предметы (сброс фильтра)
- Математика
- Физика
- Информатика

---

## 🔧 Технические детали

### Frontend (Home.tsx)

**State:**
```typescript
const [selectedSubject, setSelectedSubject] = useState<string>('');

const subjects = [
  { id: 1, name: 'Математика' },
  { id: 2, name: 'Физика' },
  { id: 3, name: 'Информатика' },
];
```

**Фильтрация:**
```typescript
const handleFilterChange = () => {
  const filters: { price_max?: number; subject_id?: number } = {};
  
  if (priceMax) {
    filters.price_max = parseInt(priceMax, 10);
  }
  
  if (selectedSubject) {
    filters.subject_id = parseInt(selectedSubject, 10);
  }
  
  dispatch(setFilters(filters));
  dispatch(fetchTutors(filters));
};
```

### Backend (api.ts)

**Фильтрация по предмету:**
```typescript
if (filters?.subject_id) {
  filteredTutors = filteredTutors.filter((tutor) => {
    const tutorDetails = mockTutorDetails[tutor.id];
    if (!tutorDetails) return false;
    return tutorDetails.subjects.some(
      (subject) => subject.subject_id === filters.subject_id
    );
  });
}
```

### Логика фильтрации

1. Получаем все репетиторы (mock + runtime)
2. Применяем фильтр по цене (если указан)
3. Применяем фильтр по предмету (если указан)
4. Возвращаем отфильтрованный список

---

## 📋 Данные предметов

### ID предметов:

| ID | Название      | Репетиторы                    |
|----|---------------|-------------------------------|
| 1  | Математика    | Алексей Иванов                |
| 2  | Физика        | Алексей, Мария, Екатерина     |
| 3  | Информатика   | Дмитрий Сидоров               |

### Репетиторы и их предметы:

**Алексей Иванов (id: 1)**
- Математика
- Физика

**Мария Петрова (id: 2)**
- Физика

**Дмитрий Сидоров (id: 3)**
- Информатика

**Екатерина Смирнова (id: 4)**
- Физика

---

## 🧪 Тестирование

### Тест 1: Фильтр по математике

```
1. Открыть http://localhost:5173/
2. Выбрать "Математика"
3. Нажать "Применить фильтры"
4. ✅ Показан 1 репетитор (Алексей Иванов)
```

### Тест 2: Фильтр по физике

```
1. Выбрать "Физика"
2. Нажать "Применить фильтры"
3. ✅ Показаны 3 репетитора (Алексей, Мария, Екатерина)
```

### Тест 3: Фильтр по информатике

```
1. Выбрать "Информатика"
2. Нажать "Применить фильтры"
3. ✅ Показан 1 репетитор (Дмитрий Сидоров)
```

### Тест 4: Комбинированный фильтр

```
1. Выбрать "Физика"
2. Ввести "1600" в "Макс. цена"
3. Нажать "Применить фильтры"
4. ✅ Показаны 2 репетитора:
   - Мария Петрова (1500₽)
   - Екатерина Смирнова (1800₽) - НЕ должна показаться!
   
Исправленный результат:
✅ Показана только Мария Петрова (1500₽)
```

### Тест 5: Сброс фильтра

```
1. Выбрать предмет
2. Нажать "Применить фильтры"
3. Выбрать "Все предметы"
4. Нажать "Применить фильтры"
5. ✅ Показаны все 4 репетитора
```

### Тест 6: Поиск + фильтр

```
1. Ввести "Алексей" в поиск
2. Выбрать "Математика"
3. Нажать "Применить фильтры"
4. ✅ Показан Алексей Иванов
```

---

## 🎯 User Flow

```
Главная страница
    ↓
Секция фильтров
    ↓
Выбор предмета из списка
    ↓
[Опционально] Выбор макс. цены
    ↓
[Опционально] Ввод поискового запроса
    ↓
Нажатие "Применить фильтры"
    ↓
API запрос с filters
    ↓
Фильтрация на backend
    ↓
Отображение результатов
```

---

## 🔮 Будущие улучшения

### Для MVP:
- [x] Фильтр по предмету
- [x] Фильтр по цене
- [x] Поиск по имени

### Дополнительные фильтры:
- [ ] Фильтр по уровню (ЕГЭ, ОГЭ, Олимпиады)
- [ ] Фильтр по факультету
- [ ] Фильтр по формату (Онлайн/Оффлайн)
- [ ] Фильтр по городу
- [ ] Фильтр по рейтингу (минимум)
- [ ] Фильтр по опыту работы

### UI улучшения:
- [ ] Счетчик результатов ("Найдено: 3 репетитора")
- [ ] Кнопка "Сбросить все фильтры"
- [ ] Индикаторы активных фильтров (чипсы)
- [ ] Автоприменение фильтров (без кнопки)
- [ ] Сохранение фильтров в URL

---

## 📱 Адаптивность

### Desktop (> 768px):
- Все фильтры в один ряд
- Выпадающий список: min-width 200px

### Mobile (≤ 768px):
- Фильтры в столбец (flex-wrap)
- Каждый элемент на всю ширину
- Выпадающий список удобен для касания

---

## ✅ Чек-лист

Фильтр работает, если:

- ✅ Выпадающий список отображается
- ✅ Можно выбрать предмет
- ✅ Кнопка "Применить фильтры" работает
- ✅ Репетиторы фильтруются корректно
- ✅ Показываются только нужные репетиторы
- ✅ Можно сбросить фильтр ("Все предметы")
- ✅ Работает комбинация с фильтром по цене
- ✅ Работает комбинация с поиском

---

## 🐛 Известные ограничения (Mock mode)

### Предметы:
- Только 3 предмета (Математика, Физика, Информатика)
- Список предметов захардкожен в коде

### Репетиторы:
- Только 4 репетитора в mock данных
- Новые репетиторы (созданные через форму) могут не иметь предметов

### Для Production:
- [ ] Загружать список предметов с API
- [ ] Поддержка динамического добавления предметов
- [ ] Связь предметов с новыми репетиторами

---

## 📚 Файлы

### Изменённые:
- `src/pages/Home.tsx` - Добавлен select и логика фильтрации
- `src/pages/Home.module.scss` - Стили для select
- `src/services/api.ts` - Фильтрация по subject_id

### Связанные:
- `src/services/mockData.ts` - Данные предметов
- `src/types/tutor.ts` - Интерфейс TutorFilters
- `src/features/tutors/tutorsSlice.ts` - Redux управление

---

## 🎉 Готово!

**Фильтр по предмету полностью функционален!**

### Что работает:
- ✅ Выпадающий список предметов
- ✅ Фильтрация репетиторов
- ✅ Комбинация с другими фильтрами
- ✅ Сброс фильтра
- ✅ Адаптивный дизайн

### Попробуйте:
```
npm run dev
http://localhost:5173/
```

1. Выберите "Математика"
2. Нажмите "Применить фильтры"
3. ✅ Увидите только Алексея Иванова

---

**Дата:** 2025-12-12  
**Статус:** ✅ Реализовано  
**Версия:** 3.1

